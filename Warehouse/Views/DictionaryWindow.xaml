<Window x:Class="Warehouse.Views.DictionaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Справочник" Height="400" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="LbxItems" Grid.Row="0" DisplayMemberPath="Name"
                 SelectionChanged="LbxItems_SelectionChanged"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Добавить" Width="90" Margin="5" Click="BtnAdd_Click"/>
            <Button Content="Изменить" Width="90" Margin="5" Click="BtnEdit_Click"/>
            <Button Content="Удалить"  Width="90" Margin="5" Click="BtnDelete_Click"/>
        </StackPanel>
    </Grid>
</Window>