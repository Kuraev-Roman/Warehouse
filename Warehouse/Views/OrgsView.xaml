<Window x:Class="Warehouse.Views.OrgsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Warehouse"
        mc:Ignorable="d"
        Title="Управление организациями" Height="450" Width="680" 
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <DataTemplate x:Key="listTemplate">
            <StackPanel Margin="0">
                <TextBlock FontFamily="Verdana" FontSize="14" 
                           Text="{Binding OrgName}" 
                           HorizontalAlignment="Stretch"/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="local:WindowCommands.OrgCancel" Executed="Exit_Executed"/>
        <CommandBinding Command="local:WindowCommands.OrgOk" Executed="Ok_Executed"/>
        <CommandBinding Command="local:WindowCommands.OrgAdd" Executed="OrgAdd_Executed"/>
        <CommandBinding Command="local:WindowCommands.OrgEdit" Executed="OrgEdit_Executed"/>
        <CommandBinding Command="local:WindowCommands.OrgDelete" Executed="OrgDelete_Executed"/>
    </Window.CommandBindings>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <DockPanel Grid.Column="0">
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="Организации">
                    <MenuItem Header="Новая организация"    Command="local:WindowCommands.OrgAdd"/>
                    <MenuItem Header="Изменить организацию" Command="local:WindowCommands.OrgEdit"/>
                    <MenuItem Header="Удалить организацию"  Command="local:WindowCommands.OrgDelete"/>
                </MenuItem>
            </Menu>
            <StatusBar DockPanel.Dock="Bottom" Background="Silver">
                <TextBlock x:Name="tbStatus" FontFamily="Verdana" FontSize="14" 
                           HorizontalAlignment="Right" TextWrapping="Wrap"/>
            </StatusBar>
            <ListBox x:Name="lbxOrgsList" 
                     ItemTemplate="{StaticResource listTemplate}" 
                     SelectionChanged="lbxOrgsList_SelectionChanged">
                <ListBox.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Новая организация"    Command="local:WindowCommands.OrgAdd"/>
                        <MenuItem Header="Изменить организацию" Command="local:WindowCommands.OrgEdit"/>
                        <MenuItem Header="Удалить организацию"  Command="local:WindowCommands.OrgDelete"/>
                    </ContextMenu>
                </ListBox.ContextMenu>
            </ListBox>
        </DockPanel>
        <StackPanel Grid.Column="1" Margin="10">
            <Button Content="OK"      Command="local:WindowCommands.OrgOk"     Width="80" Height="30" Margin="5"/>
            <Button Content="Закрыть" Command="local:WindowCommands.OrgCancel" Width="80" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>