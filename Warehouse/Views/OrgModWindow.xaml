<Window x:Class="Warehouse.Views.OrgModWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Организация" Height="180" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label x:Name="lblName" Grid.Row="0" FontFamily="Verdana" FontSize="14" Content="Наименование компании:"/>
        <TextBox x:Name="tbName" Grid.Row="1" FontFamily="Verdana" FontSize="14" Margin="0,5"/>
        <TextBlock x:Name="tbHint" Grid.Row="2" FontFamily="Verdana" FontSize="11" 
                   Foreground="Red" Margin="0,2" Visibility="Collapsed"
                   TextWrapping="Wrap"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5">
            <Button Content="OK"     Width="80" Height="30" Margin="5,0" Click="Ok_Click"/>
            <Button Content="Отмена" Width="80" Height="30" Margin="5,0" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>